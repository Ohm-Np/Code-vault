# using modules in python
# a module is essentially just a python file that we can ...
# ... import into current python file

import tools_for_modules
print(tools_for_modules.roll_dice(10))

# pip - package manager (preferred installer program)
# install, manage, update and uninstall different package module

